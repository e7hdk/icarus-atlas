/** MapLibre label font stack — Cinzel, the atlas display serif. Self-hosted SDF
 *  glyphs are baked by scripts/build-map-fonts.ts into public/geo/font/Cinzel/;
 *  the style's `glyphs` URL points there, so this name must stay 'Cinzel'. */
export const MAP_LABEL_FONT = ['Cinzel'] as const;
